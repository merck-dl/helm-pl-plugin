## `config` variabled used


```yaml
# config
git_upload:
    enabled:
    user:
    email:
    git_repo_with_access_token:


smart_contracts:

network_name:

git_shared_configuration:
    read_write_token:
    repository_name:
```